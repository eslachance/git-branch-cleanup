{
  "name": "git-branch-cleanup",
  "version": "1.0.3",
  "main": "src/index.js",
  "bin": {
    "git-branch-cleanup": "src/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Eric-Sebastien Lachance <eslachance@gmail.com> (https://alterion.dev/)",
  "description": "A Node.js CLI tool to help clean up your local Git branches that have no active upstream branch or whose upstream branch is gone.",
  "dependencies": {
    "prompt": "^1.3.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">= 6.0.0"
  },
  "publishConfig": {
   "registry": "https://registry.npmjs.org"
  }
}
